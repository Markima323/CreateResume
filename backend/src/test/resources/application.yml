spring:
  datasource:
    url: jdbc:h2:mem:resume;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  openai:
    api-key: test-key
    model: test-model
    base-url: http://localhost:9999/v1
  generation-dir: ./target/test-generations
